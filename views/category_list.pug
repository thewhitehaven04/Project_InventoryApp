extends layout 

block content 
  h1 Categories 

  div.container 
    ul(class="list-group")
      each category in categories 
        li(class="list-group-item")
          a(href=category._id)= category.name

    a(href='/category/new')
      button(type="button" class="btn btn-outline-primary") Add new category 
