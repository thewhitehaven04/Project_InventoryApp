extends layout 

block content 
  h1 Categories 

  ul(class="list-group")
    each ageCategory in ageCategories 
      li.list-group-item
        a(href=ageCategory._id)= ageCategory.name

    a(href='/ageCategory/new')
      button(type="button" class="border-2 border-solid border-indigo-600 rounded p-2") Add new category 
